---
description: OSCAL import workspace guardrails
paths:
  - "workspaces/**"
  - "examples/legacy-ssp-to-oscal/**"
---

When working in an OSCAL import workspace:

- Preserve original source files in `input/` unchanged.
- Maintain `source-map.csv` traceability for every mapped OSCAL field.
- Mark uncertain mappings as `needs_review`; do not invent system boundaries, control implementations, or authorization conclusions.
- Do not commit real customer SSPs, credentials, diagrams, or sensitive evidence.
- Produce import summary, validation report, and unmapped/review-queue artifacts before claiming success.
- Schema-valid OSCAL is structural validation only, not an audit opinion.

Use `/import-legacy-ssp` or `agent-skills/oscal-document-engineering/SKILL.md` for the full workflow.
